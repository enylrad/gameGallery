// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {

    // Define versions in a single place
    ext {
        // SDK and tools
        compileSdkVersion = 29
        minSdkVersion = 21
        targetSdkVersion = 29

        kotlinVersion = '1.4.31'

        // App dependencies
        appCompatVersion = '1.2.0'
        recyclerViewVersion = '1.1.0'
        constraintLayoutVersion = '2.0.4'
        coroutinesVersion = '1.3.3'
        glideVersion = '4.12.0'
        navigationVersion = '2.3.4'
        navigationArgsVersion = '1.0.0'
        ktxVersion = '1.3.2'
        materialVersion = '1.3.0'
        roomVersion = '2.2.6'
        roomViewerVersion = '1.0.6'
        workVersion = '2.5.0'
        pagingVersion = '2.1.2'
        lifeCycleVersion = '2.2.0'
        koinVersion = '3.0.1-beta-1'
        retrofitVersion = '2.9.0'
        retrofitScalarsVersion = '2.9.0'
        okhttpVersion = '5.0.0-alpha.2'
        firebaseBom = '26.7.0'
        fbCrashlyticsGradleVersion = '2.1.1'
        fbPerfPluginVersion = '1.3.1'
        psAuthVersion = '19.0.0'
        psPerfVersion = '1.3.1'
        lottieVersion = '3.6.1'
        chuckVersion = '1.1.0'
        timberVersion = '4.7.1'
        circleImgVersion = '3.1.0'
        gradleVersion = '4.1.2'
        safeArgsVersion = '1.0.0'
        fbPluginsVersion = '2.0.0'
        aptVersion = '1.8'
        gServicesVersion = '4.3.3'

        // Sonar
        sonarVersion = '2.7.1'

        // Testing
        junitVersion = '4.13.2'
        espressoVersion = '3.3.0'
        coreTestingVersion = '2.1.0'
        jacocoVersion = '0.8.4'
        runnerVersion = '1.0.1'
        uiAutomatorVersion = '2.2.0'
        mockitoVersion = '2.25.0'
        mockitoAllVersion = '1.10.19'
        mockitoAndroidVersion = '2.25.0'
        mockwebserverVersion = '5.0.0-alpha.2'
        testExtJunit = '1.1.2'
        testCoreVersion = '1.3.0'
        testRunnerVersion = '1.3.0'
        testRulesVersion = '1.3.0'

    }

    repositories {
        google()
        mavenCentral()
        maven { url 'https://plugins.gradle.org/m2/' }
    }

    dependencies {
        classpath "com.android.tools.build:gradle:${gradleVersion}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "org.jetbrains.kotlin:kotlin-allopen:$kotlinVersion"
        classpath "com.neenbedankt.gradle.plugins:android-apt:$aptVersion"

        // Arch
        classpath "android.arch.navigation:navigation-safe-args-gradle-plugin:$navigationArgsVersion"

        // Firebase
        classpath "com.google.firebase:firebase-plugins:$fbPluginsVersion"
        classpath "com.google.firebase:firebase-crashlytics-gradle:$fbCrashlyticsGradleVersion"
        classpath "com.google.firebase:perf-plugin:$fbPerfPluginVersion"

        // Google Services
        classpath "com.google.gms:google-services:$gServicesVersion"

        // Sonar
        classpath "org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:$sonarVersion"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}