package es.enylrad.gamesgallery

import android.app.Application

/**
 * App for tests to prevent initializing dependency injection.
 *
 * See [es.enylrad.gamesgallery.commons.utils.AppTestRunner].
 */
class TestApp : Application()
